# Eolia

Static mirror of the Eolia clean energy landing site with refreshed AI-generated imagery.

The production content images are exported from the ChatGPT-generated masters in
`source-assets/chatgpt` using:

```bash
python scripts/export-images.py
```

UI icons, arrows, and logo marks are kept from the original interface.
